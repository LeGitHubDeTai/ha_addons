name: Manual Build

on:
  workflow_dispatch:
    inputs:
      target_addon:
        description: 'Addon spécifique à builder (laisser vide pour tous)'
        required: false
        default: ''
      force_rebuild:
        description: 'Forcer le rebuild même si l image existe'
        required: false
        default: false
        type: boolean

env:
  BUILD_ARGS: ""

jobs:
  # 1. Get all addons or specific addon
  get-addons:
    runs-on: ubuntu-latest
    outputs:
      addons: ${{ steps.get_addons.outputs.addons }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Get addons to build
        id: get_addons
        run: |
          if [ -n "${{ github.event.inputs.target_addon }}" ]; then
            # Build specific addon
            addon="${{ github.event.inputs.target_addon }}"
            if [ -d "$addon" ] && [ -f "$addon/config.yaml" ]; then
              echo "addons=[\"$addon\"]" >> "$GITHUB_OUTPUT"
              echo "Building specific addon: $addon"
            else
              echo "Addon $addon not found"
              echo "addons=[]" >> "$GITHUB_OUTPUT"
            fi
          else
            # Build all addons
            addons="["
            first=true
            for dir in */; do
              addon_name=$(basename "$dir")
              if [ -f "$dir/config.yaml" ] && [ -f "$dir/Dockerfile" ]; then
                if [ "$first" = true ]; then
                  addons="$addons\"$addon_name\""
                  first=false
                else
                  addons="$addons,\"$addon_name\""
                fi
              fi
            done
            addons="$addons]"
            echo "addons=$addons" >> "$GITHUB_OUTPUT"
            echo "Building all addons: $addons"
          fi

  # 2. Build images
  build:
    if: ${{ needs.get-addons.outputs.addons != '' }}
    needs: get-addons
    runs-on: ubuntu-latest
    environment: production
    name: Build ${{ matrix.arch }} ${{ matrix.addon }} add-on
    strategy:
      fail-fast: false
      matrix:
        addon: [mopidy, n8n, obsidian, planka, syncthing, dolibarr, FossFLOW]
        arch: ["aarch64", "amd64", "armv7", "armhf", "i386"]
    steps:
      - name: Check if addon should be built
        id: should_build
        run: |
          if [ -n "${{ github.event.inputs.target_addon }}" ]; then
            if [ "${{ matrix.addon }}" = "${{ github.event.inputs.target_addon }}" ]; then
              echo "Addon ${{ matrix.addon }} should be built (target specified)"
              echo "::set-output name=build::true"
            else
              echo "Addon ${{ matrix.addon }} not target, skipping"
              echo "::set-output name=build::false"
            fi
          else
            echo "Addon ${{ matrix.addon }} should be built (all addons)"
            echo "::set-output name=build::true"
          fi
      
      - uses: actions/checkout@v4
        if: steps.should_build.outputs.build == 'true'
      
      - name: Get information
        id: info
        if: steps.should_build.outputs.build == 'true'
        uses: home-assistant/actions/helpers/info@master
        with:
          path: "./${{ matrix.addon }}"
      
      - name: Check if Dockerfile exists
        id: dockerfile_check
        if: steps.should_build.outputs.build == 'true'
        run: |
          if [ -f "./${{ matrix.addon }}/Dockerfile" ]; then
            echo "has_dockerfile=true" >> "$GITHUB_OUTPUT"
          else
            echo "No Dockerfile found in ${{ matrix.addon }}, skipping build."
            echo "has_dockerfile=false" >> "$GITHUB_OUTPUT"
          fi
      
      - name: Check if add-on should be built for arch
        id: check
        if: steps.should_build.outputs.build == 'true'
        run: |
          # Vérifier si l'architecture est supportée par HA Builder
          case "${{ matrix.arch }}" in
            "amd64"|"aarch64")
              builder_supported=true
              ;;
            *)
              builder_supported=false
              echo "Architecture ${{ matrix.arch }} non supportée par HA Builder, utilisation de buildx manuel"
              ;;
          esac
          
          # Vérifier si l'architecture est dans la liste de l'addon
          if [[ "${{ steps.info.outputs.architectures }}" =~ ${{ matrix.arch }} ]]; then
             echo "build_arch=true" >> "$GITHUB_OUTPUT";
             echo "builder_supported=$builder_supported" >> "$GITHUB_OUTPUT";
             echo "image=$(echo "${{ steps.info.outputs.image }}" | cut -d'/' -f3)" >> "$GITHUB_OUTPUT";
          else
             echo "${{ matrix.arch }} is not a valid arch for ${{ matrix.addon }}, skipping build";
             echo "build_arch=false" >> "$GITHUB_OUTPUT";
          fi
      
      - name: Login to GitHub Container Registry
        if: steps.should_build.outputs.build == 'true'
        uses: docker/login-action@v4.0.0
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build ${{ matrix.addon }} add-on (HA Builder)
        id: builderstep
        if: steps.check.outputs.build_arch == 'true' && steps.check.outputs.builder_supported == 'true' && steps.dockerfile_check.outputs.has_dockerfile == 'true' && steps.should_build.outputs.build == 'true'
        uses: home-assistant/builder@2026.02.1
        with:
          args: |
            ${{ env.BUILD_ARGS }} \
            --${{ matrix.arch }} \
            --target "/data/${{ matrix.addon }}" \
            --image "${{ matrix.addon }}" \
            --docker-hub "ghcr.io/${{ github.repository_owner }}/ha_addons" \
            --addon
      
      - name: Build ${{ matrix.addon }} add-on (Docker Buildx)
        if: steps.check.outputs.build_arch == 'true' && steps.check.outputs.builder_supported == 'false' && steps.dockerfile_check.outputs.has_dockerfile == 'true' && steps.should_build.outputs.build == 'true'
        run: |
          echo "Build avec Docker Buildx pour ${{ matrix.arch }} (non supporté par HA Builder)"
          
          # Récupérer la version depuis config.yaml
          if [ -f "${{ matrix.addon }}/config.yaml" ]; then
            version=$(grep '^version:' "${{ matrix.addon }}/config.yaml" | sed 's/version: "//' | sed 's/"//' | tr -d '\r\n' || echo "latest")
          else
            version="latest"
          fi
          
          # Récupérer l'image de base depuis build.yaml si disponible
          if [ -f "${{ matrix.addon }}/build.yaml" ]; then
            base_image=$(grep "${{ matrix.arch }}:" "${{ matrix.addon }}/build.yaml" | sed 's/^[^:]*:\s*//' | sed 's/^"//' | sed 's/"$//' | tr -d '\r\n' | tr -d '"')
            if [ -z "$base_image" ]; then
              # Fallback vers une image de base par défaut
              case "${{ matrix.arch }}" in
                "armv7") base_image="ghcr.io/home-assistant/armv7-base:3.18" ;;
                "armhf") base_image="ghcr.io/home-assistant/armhf-base:3.18" ;;
                "i386") base_image="ghcr.io/home-assistant/i386-base:3.18" ;;
                *) base_image="ghcr.io/home-assistant/amd64-base:3.18" ;;
              esac
            fi
          else
            base_image="ghcr.io/home-assistant/amd64-base:3.18"
          fi
          
          echo "Image de base: $base_image"
          echo "Version: $version"
          
          # Convertir l'architecture pour Docker
          case "${{ matrix.arch }}" in
            "armv7") docker_arch="linux/arm/v7" ;;
            "armhf") docker_arch="linux/armhf" ;;
            "i386") docker_arch="linux/386" ;;
            *) docker_arch="linux/${{ matrix.arch }}" ;;
          esac
          
          # Construire avec buildx
          if docker buildx build \
            --platform "$docker_arch" \
            --push \
            --tag "ghcr.io/${{ github.repository_owner }}/ha_addons/${{ matrix.addon }}-${{ matrix.arch }}:$version" \
            --tag "ghcr.io/${{ github.repository_owner }}/ha_addons/${{ matrix.addon }}-${{ matrix.arch }}:latest" \
            --build-arg BUILD_FROM="$base_image" \
            --build-arg BUILD_VERSION="$version" \
            --cache-from "type=gha" \
            --cache-to "type=gha,mode=max" \
            "./${{ matrix.addon}}"; then
            echo "✅ Build réussi pour ${{ matrix.addon }} (${{ matrix.arch }}) - $version"
          else
            echo "❌ Build échoué pour ${{ matrix.addon }} (${{ matrix.arch }})"
            exit 1
          fi
